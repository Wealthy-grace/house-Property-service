apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: property-service-monitor
  namespace: monitoring
  labels:
    app: property-service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: property-service
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
      scrapeTimeout: 10s
  namespaceSelector:
    matchNames:
      - student-housing